package database

import "database/sql"

//структура хранит указатель на подключение к базе данных
//Иначе во многих местах кода придётся подключатся к БД - sql.Open("postgres", connDB)
type App struct {
	db *sql.DB
}
<<<<<<< HEAD

// NewApp создаёт App поверх уже готового *sql.DB.
//
// Основная причина существования этой функции — тестируемость:
// ConnectDB() сам открывает реальное соединение с PostgreSQL и не подходит
// для юнит-тестов (они не должны трогать сеть/БД). NewApp же позволяет
// подставить любую реализацию *sql.DB, включая ту, что возвращает
// sqlmock.New() — так тесты в internal/database проверяют именно то,
// какой SQL мы отправляем и как обрабатываем результат/ошибку,
// а не поднимают настоящий Postgres.
func NewApp(db *sql.DB) *App {
	return &App{db: db}
}
=======
>>>>>>> fece3f61e8bfdcddb1ea730537dcd69de87ea122
